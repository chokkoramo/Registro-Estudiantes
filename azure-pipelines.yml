trigger:
  - master

pool:
  name: Default

steps:
  - script: chmod +x gradlew
    displayName: 'Dar permisos a Gradle'

  - script: ./gradlew clean build
    displayName: 'Build proyecto'

  # Tests normales
  - script: ./gradlew test
    displayName: 'Correr tests'

  # Mutation testing con PIT
  - script: ./gradlew pitest
    displayName: 'Prueba de mutacion (PIT)'

  - script: ./gradlew sonar
    displayName: 'Analisis en sonar'